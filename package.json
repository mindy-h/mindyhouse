{
  "name": "figma-design-implementation",
  "version": "1.0.0",
  "description": "Implementation of Figma design for Martech component library",
  "main": "index.js",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && cp -r dist/* docs/ && git add docs && git commit -m 'Deploy' && git push"
  },
  "homepage": "https://mindy-h.github.io/mindyhouse",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^4.4.0"
  }
}
